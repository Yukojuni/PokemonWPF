<UserControl x:Class="PokemonWPF.View.Setting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokemonWPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400" Background="White">

    <StackPanel VerticalAlignment="Center">
        <TextBox HorizontalAlignment="Center" VerticalAlignment="Center" Height="40" Width="350" FontSize="8"
                 Text="{Binding ConnectionString}" TextChanged="TextBox_TextChanged" />
        <Button Content="Add" Width="75" Height="30" Margin="0,5,0,0"
         Command="{Binding ReloadCommand}"/>
    </StackPanel>
</UserControl>
